# Ingestion adapter — minimal, deliberately
httpx==0.27.2          # HTTP client; sync + async, better timeout handling than requests
pytest==8.3.3
pytest-httpx==0.30.0   # mock OAI responses without a live Koha
psycopg[binary]==3.2.3  # PostgreSQL driver; binary avoids a build toolchain
