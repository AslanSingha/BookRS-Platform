# Embedding service. Kept separate from requirements-dev.txt because
# sentence-transformers pulls in PyTorch -- roughly 2 GB of image layers
# the ingestion service has no use for. The separation runs one way: it
# keeps PyTorch out of the ingestion image, not httpx out of this one.
sentence-transformers==3.3.1
psycopg[binary]==3.2.3


# Test-only. Present so the whole suite runs in either container --
# a suite that needs two commands is a suite someone forgets to run
# half of.
pytest==8.3.3
httpx==0.27.2
pytest-httpx==0.30.0
